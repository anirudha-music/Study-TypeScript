// null
// Need to enable strictNullChecks in the tsconfig.json file.
let my_age: number | null = 20
console.log(my_age)

my_age = null
console.log(my_age)

let canBeNull;
canBeNull = null

let nullValue = null
nullValue = 20