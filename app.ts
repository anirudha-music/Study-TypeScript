let my_name = 'Anirudha';
console.log(my_name);

my_name = 'Ani';
console.log(my_name);

// This here will throw an error while you convert the the file to js as you can't change the variable type once it is declared or initialised in typescript.
my_name = 2018;


