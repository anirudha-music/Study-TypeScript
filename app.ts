// string
let my_name = 'Anirudha';

// number
let my_age = 25;

// boolean
let hasHobbies = true;

const hobbies = ['Reading', 'Travelling', 'Eating'];

let allowed_alphabets = ['a', 'b', 'c', 'd'];

let person: [string, number] = ['Anirudha', 25];

enum Colors {
    red,
    green,
    blue
}
let color = Colors.green;